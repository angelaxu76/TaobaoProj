【helper/ — 独立小工具层】
规则：不依赖业务逻辑的独立工具脚本，通常手动执行一次性任务。

  image/  →  图片转换（HEIC/AVIF/WebP→JPG）、裁剪、缩放、水印、合并、去指纹
  excel/  →  Excel 查重、拆分、导出ID列表
  html/   →  HTML → PNG 渲染
  txt/    →  TXT 文件处理（提取标题、移除已处理ID）
